<h1>Park : <%= @park.name %> </h1>

<div class="rides">
  <% @park.rides.each do |ride| %>
    <p><%= ride.name %></p>
  <% end %>
  <p>Average Thrill Rating of Rides: <%= @park.rides.average_rating.round(1) %>/10 </p>
</div>
